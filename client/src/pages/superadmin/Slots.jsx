import { PageContainer, ComingSoon } from '../../components/common';

export default function Slots() {
  return (
    <PageContainer title="Slots" description="Manage class timing slots">
      <ComingSoon label="Slots" />
    </PageContainer>
  );
}
