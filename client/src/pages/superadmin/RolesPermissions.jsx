import { PageContainer, ComingSoon } from '../../components/common';

export default function RolesPermissions() {
  return (
    <PageContainer title="Roles & Permissions" description="Configure role-based access control">
      <ComingSoon label="Roles & Permissions" />
    </PageContainer>
  );
}
