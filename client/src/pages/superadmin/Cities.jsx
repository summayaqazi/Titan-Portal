import { PageContainer, ComingSoon } from '../../components/common';

export default function Cities() {
  return (
    <PageContainer title="Cities" description="Manage cities where campuses are located">
      <ComingSoon label="Cities" />
    </PageContainer>
  );
}
